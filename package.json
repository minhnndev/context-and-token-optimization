{
  "name": "tokenlens-for-copilot",
  "displayName": "TokenLens for GitHub Copilot",
  "description": "Local-first AI credit sizing, Copilot CLI usage, cache continuity, and GitHub calibration for VS Code.",
  "version": "0.3.0",
  "publisher": "minhnndev",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/minhnndev/context-and-token-optimization.git"
  },
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "The extension reads Git metadata and the local Copilot CLI session store."
    },
    "virtualWorkspaces": {
      "supported": false,
      "description": "A filesystem-backed Git workspace is required."
    }
  },
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Other",
    "Visualization",
    "Machine Learning"
  ],
  "keywords": [
    "copilot",
    "tokens",
    "cost",
    "cache",
    "github"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "compile": "npm run clean && tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "tsc -p ./ --noEmit",
    "test:core": "npm run compile && node --test dist/test/*.test.js",
    "test:legacy": "node --test scripts/test/*.test.mjs plugins/cache-continuity/test/*.test.mjs console/test/*.test.mjs",
    "test": "npm run test:core && npm run test:legacy",
    "package": "vsce package --no-dependencies",
    "vscode:prepublish": "npm run compile"
  },
  "contributes": {
    "commands": [
      {
        "command": "tokenLens.startTask",
        "title": "TokenLens: Start Task",
        "icon": "$(play)"
      },
      {
        "command": "tokenLens.estimateTask",
        "title": "TokenLens: Estimate Task",
        "icon": "$(wand)"
      },
      {
        "command": "tokenLens.completeTask",
        "title": "TokenLens: Complete Task",
        "icon": "$(pass-filled)"
      },
      {
        "command": "tokenLens.refreshUsage",
        "title": "TokenLens: Refresh Usage",
        "icon": "$(refresh)"
      },
      {
        "command": "tokenLens.openMetrics",
        "title": "TokenLens: Open Metrics",
        "icon": "$(graph-line)"
      },
      {
        "command": "tokenLens.openHistory",
        "title": "TokenLens: View History",
        "icon": "$(history)"
      },
      {
        "command": "tokenLens.syncTask",
        "title": "TokenLens: Sync Task to GitHub",
        "icon": "$(cloud-upload)"
      },
      {
        "command": "tokenLens.openSettings",
        "title": "TokenLens: Open Settings",
        "icon": "$(settings-gear)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "tokenLens",
          "title": "TokenLens",
          "icon": "media/activity.svg"
        }
      ]
    },
    "views": {
      "tokenLens": [
        {
          "id": "tokenLens.session",
          "name": "Current Session"
        },
        {
          "id": "tokenLens.task",
          "name": "Current Task"
        },
        {
          "id": "tokenLens.history",
          "name": "History"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "tokenLens.session",
        "contents": "No Copilot CLI usage found for this workspace.\n[Refresh usage](command:tokenLens.refreshUsage)"
      },
      {
        "view": "tokenLens.task",
        "contents": "No active task.\n[Start a task](command:tokenLens.startTask)\n[Estimate without starting](command:tokenLens.estimateTask)"
      },
      {
        "view": "tokenLens.history",
        "contents": "Completed tasks stay local until you choose to sync them."
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "tokenLens.openMetrics",
          "when": "viewContainer == tokenLens",
          "group": "navigation@1"
        },
        {
          "command": "tokenLens.refreshUsage",
          "when": "view == tokenLens.session",
          "group": "navigation@2"
        }
      ],
      "explorer/context": [
        {
          "command": "tokenLens.estimateTask",
          "group": "navigation@20"
        }
      ]
    },
    "configuration": {
      "title": "TokenLens",
      "properties": {
        "tokenLens.creditUsd": {
          "type": "number",
          "default": 0.01,
          "minimum": 0,
          "description": "USD value of one AI credit. Used for display only when the provider already reports credits."
        },
        "tokenLens.pollIntervalSeconds": {
          "type": "number",
          "default": 5,
          "minimum": 2,
          "maximum": 300,
          "description": "How often to read new Copilot CLI usage events."
        },
        "tokenLens.cacheNotifications": {
          "type": "boolean",
          "default": true,
          "description": "Notify when cache reuse changes significantly or the model configuration changes."
        },
        "tokenLens.significantCacheDeltaTokens": {
          "type": "number",
          "default": 4096,
          "minimum": 0,
          "description": "Absolute cache-read token change that counts as significant."
        },
        "tokenLens.githubApiBaseUrl": {
          "type": "string",
          "default": "https://api.github.com",
          "description": "GitHub REST API base URL. GitHub Enterprise authentication is not yet supported by the MVP."
        }
      }
    }
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.101.0",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^5.8.3"
  }
}
